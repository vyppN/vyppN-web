import {CardContainer} from '@libs/elements/containers/CardContainer/card-container.element'
import styled from 'styled-components'

export const PackageCardContainer = styled(CardContainer)`
height: 241px;
break-inside: avoid-column;
page-break-inside: avoid;
flex: 1;
`
